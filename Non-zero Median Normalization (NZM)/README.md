非零中位数归一化是一种基于每个样本中正值（非零）数据的中位数来调整样本测量值的缩放方法。它通过所有样本的中位数平均值作为全局参考，将不同样本的测量值对齐到同一尺度。该算法适用于组学中信号数据，其中零值代表缺失或未检测到的特征，不应影响归一化过程。

适用场景：
数据中包含大量零值，且这些零值不应主导缩放（例如：插补后的缺失值、稀疏表达谱数据）。

不同样本的量纲或绝对丰度差异较大，但正值信号间的相对比例具有可比性。

需要一种对离群值不敏感的稳健归一化方法（峰值peak处信号极高，使用中位数而非均值对样本内部进行缩放）。
